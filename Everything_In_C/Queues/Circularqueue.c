#include <stdio.h>
struct Node{
    int data;
    struct Node *next;
}*first,*last,*temp;

void enqueue(int value){
    temp -> data = value;
    temp -> next = NULL;

    if(first == NULL){
        first = last = NULL;
        last ->
        

    }

}
int main(){
    
    return 0;
}